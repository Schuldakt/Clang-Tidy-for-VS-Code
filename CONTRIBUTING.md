# Contributing to Clang-Tidy for VS Code

We welcome contributions!

1. **Build the project**: Run `pnpm install` then `pnpm run build`.
2. **Test**: Use the "Run Extension" debug task in VS Code.
3. **Pull Requests**: Please keep PRs small and focused on one feature or bug fix.
